import React from "react";
import styles from "./Form.module.scss";

function Form() {
  return (
    <section className={styles.sectionForm}>
      <div>Form</div>
    </section>
  );
}

export default Form;
